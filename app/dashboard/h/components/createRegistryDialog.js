"use client";
import RegistryBuilderDialog from "./registry-builder/RegistryBuilderDialog";

export default function CreateRegistryDialog({ onClose }) {
  return <RegistryBuilderDialog onClose={onClose} />;
}
